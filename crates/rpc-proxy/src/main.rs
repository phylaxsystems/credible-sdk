//! Entry point for the Credible RPC proxy.
//! Currently this crate only provides a placeholder binary until the
//! sidecar-aware heuristics are implemented.

fn main() {
    println!("credible rpc proxy placeholder");
}
