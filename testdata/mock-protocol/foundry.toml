[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200

[profile.assertions]
src = "assertions/src"
out = "out"
test = "assertions/test"
optimizer = true
optimizer_runs = 200

[lint]
exclude_lints = ["asm-keccak256"]
